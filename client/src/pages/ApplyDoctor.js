import React from "react";
import Layout from "../components/Layout";

const ApplyDoctor = () => {
     return (
          <Layout>
               <h1>Apply Doctor</h1>
          </Layout>
     );
};

export default ApplyDoctor;
